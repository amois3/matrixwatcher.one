# 🚀 Инструкция по запуску Oracle Instance Auto-Creator

## Шаг 1: Настрой конфигурацию (1 минута)

```bash
bash setup_oracle_config.sh
```

## Шаг 2: Установи Oracle SDK (2 минуты)

```bash
pip3 install oci
```

## Шаг 3: Запусти автоматический создатель инстанса

```bash
python3 oracle_instance_creator.py
```

Скрипт будет пытаться создать инстанс каждые 10 минут во всех 3 availability domains.

Когда инстанс создастся, скрипт выведет:
- Instance ID
- Public IP
- SSH команду для подключения

## Что делает скрипт:

1. ✅ Автоматически находит образ Ubuntu 22.04
2. ✅ Создаёт/использует VCN и subnet
3. ✅ Пробует все 3 availability domains (AD-1, AD-2, AD-3)
4. ✅ Повторяет попытки каждые 10 минут
5. ✅ Автоматически настраивает SSH ключ
6. ✅ Получает публичный IP

## После создания инстанса:

Скрипт остановится и выведет SSH команду типа:
```
ssh -i /Users/amois/Downloads/amoisejevs3@gmail.com-2026-01-19T11_45_18.939Z.pem ubuntu@<PUBLIC_IP>
```

## ⚠️ ВАЖНО: После успешного создания инстанса

1. Удали приватный ключ из чата (уже сделано)
2. Удали API ключ в Oracle Cloud Console
3. Удали файлы:
   - `~/.oci/oracle_api_key.pem`
   - `/Users/amois/Downloads/amoisejevs3@gmail.com-2026-01-19T11_45_18.939Z.pem`

## Остановка скрипта

Нажми `Ctrl+C` в любой момент
