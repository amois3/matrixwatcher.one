# 🤖 Автоматическая калибровка системы

## 🎯 Что это

Система **автоматически** анализирует накопленные данные и предлагает (или применяет) оптимальные пороги.

**Чем дольше работает → тем точнее становится!**

---

## ✨ Как это работает

### 1. Сбор данных (автоматически)

Система логирует:
- Все проверки порогов
- Все значения параметров
- Частоту срабатываний
- Распределения значений

### 2. Анализ (раз в день)

Каждые 24 часа система:
- Проверяет, достаточно ли данных (30 дней минимум)
- Анализирует каждый порог
- Рассчитывает оптимальные значения
- Оценивает уверенность (high/medium/low)

### 3. Рекомендации или применение

**Режим "suggest" (по умолчанию):**
- Система предлагает изменения
- Отправляет уведомление в Telegram
- Сохраняет отчёт в `logs/calibration/`
- Ждёт ручного подтверждения

**Режим "auto-apply" (опционально):**
- Система автоматически применяет изменения с высокой уверенностью
- Остальные требуют подтверждения

---

## 📊 Алгоритм калибровки

### Для каждого порога:

```python
# 1. Собираем статистику
total_checks = 5000
triggered = 250
trigger_rate = 250 / 5000 = 5%

# 2. Рассчитываем перцентили
values = [все наблюдаемые значения]
P90 = 90-й перцентиль
P95 = 95-й перцентиль
P99 = 99-й перцентиль

# 3. Определяем оптимальный порог
target_trigger_rate = 2%  # Целевая частота

if trigger_rate > 4%:  # Слишком много срабатываний
    new_threshold = P95  # Сделать строже
elif trigger_rate < 1%:  # Слишком мало
    new_threshold = P90  # Сделать мягче
else:
    # Оптимально, не менять
    pass

# 4. Оцениваем уверенность
if total_checks >= 5000 and trigger_rate сильно отличается:
    confidence = "high"  # Можно применять автоматически
elif total_checks >= 2000:
    confidence = "medium"  # Требует проверки
else:
    confidence = "low"  # Недостаточно данных
```

---

## 🚀 Использование

### Режим 1: Suggest only (по умолчанию)

```python
# В main.py уже настроено:
self.auto_calibrator = get_auto_calibrator(auto_apply=False)
```

**Что происходит:**
1. Система анализирует данные раз в день
2. Отправляет рекомендации в Telegram
3. Сохраняет отчёт в `logs/calibration/calibration_report_*.json`
4. Ждёт ручного применения

**Как применить вручную:**
```bash
# 1. Посмотреть отчёт
cat logs/calibration/calibration_report_*.json | jq

# 2. Если согласен - обновить config.json или код
# 3. Перезапустить систему
```

### Режим 2: Auto-apply (для продвинутых)

```python
# В main.py изменить:
self.auto_calibrator = get_auto_calibrator(auto_apply=True)
```

**Что происходит:**
1. Система анализирует данные раз в день
2. Автоматически применяет изменения с **high confidence**
3. Остальные отправляет как рекомендации
4. Уведомляет в Telegram о всех изменениях

⚠️ **Внимание:** Используй только если уверен в системе!

---

## 📈 Пример работы

### День 1-29: Сбор данных

```
🔬 Auto-calibration: 25.3 days until ready
```

Система молча собирает данные.

### День 30: Первая калибровка

```
🔬 Running auto-calibration check...
✅ Auto-calibration completed: Проанализировано 15 порогов. 
   Рекомендовано изменить 3 порога.
```

**Telegram уведомление:**
```
🔬 Автокалибровка завершена

Проанализировано 15 порогов. Рекомендовано изменить 3 порога.

Рекомендации:

🟢 quantum_rng › randomness_score › min
  Текущий: 0.9500
  Рекомендуемый: 0.9300 (-2.1%)
  Причина: Trigger rate too high (15.3%), using P95

🟡 crypto › BTCUSDT › price › change_pct
  Текущий: 2.5000
  Рекомендуемый: 2.8000 (+12.0%)
  Причина: Trigger rate high (4.2%), using P95

🟢 earthquake › max_magnitude › trigger_above
  Текущий: 4.5000
  Рекомендуемый: 4.3000 (-4.4%)
  Причина: Trigger rate too low (0.3%), using P90

📊 Полный отчёт: logs/calibration/calibration_report_1234567890.json
```

### День 31+: Непрерывное улучшение

Каждые 30 дней система пересматривает пороги на основе новых данных.

**Система становится точнее со временем!**

---

## 📊 Проверка статуса

### Через HTTP API

```bash
curl http://localhost:8080/health | jq .calibration
```

**Ответ:**
```json
{
  "ready_for_calibration": true,
  "days_collecting": 32.5,
  "days_needed": 30,
  "auto_apply_enabled": false,
  "last_calibration": {
    "timestamp": 1234567890,
    "days_of_data": 30.2,
    "results": {
      "thresholds_analyzed": 15,
      "recommendations": 3
    }
  },
  "total_calibrations": 1
}
```

### Через логи

```bash
tail -f matrix_watcher.log | grep calibration
```

---

## 📝 Отчёты калибровки

### Структура отчёта

```json
{
  "status": "completed",
  "timestamp": 1234567890,
  "thresholds_analyzed": 15,
  "recommendations": [
    {
      "threshold_name": "quantum_rng.randomness_score.min",
      "current_value": 0.95,
      "recommended_value": 0.93,
      "change_percent": -2.1,
      "confidence": "high",
      "reason": "Trigger rate too high (15.3%), using P95",
      "stats": {
        "total_checks": 5234,
        "current_trigger_rate": 0.153,
        "expected_trigger_rate": 0.05,
        "percentiles": {
          "p50": 0.951,
          "p90": 0.930,
          "p95": 0.930,
          "p99": 0.925
        }
      }
    }
  ],
  "auto_applied": [],
  "summary": "Проанализировано 15 порогов. Рекомендовано изменить 3 порога."
}
```

---

## 🎓 Как система становится точнее

### Месяц 1: Базовая калибровка

```
Данных: 30 дней
Точность: Базовая
Пороги: Оптимизированы под текущие условия
```

### Месяц 2-3: Учёт сезонности

```
Данных: 60-90 дней
Точность: Средняя
Пороги: Учитывают краткосрочные тренды
```

### Месяц 4-6: Стабилизация

```
Данных: 120-180 дней
Точность: Высокая
Пороги: Учитывают сезонность, тренды, корреляции
```

### Год+: Полная калибровка

```
Данных: 365+ дней
Точность: Максимальная
Пороги: Полностью адаптированы под реальность
Вероятности: Честные, с учётом всех коррекций
```

---

## 🔧 Настройка

### Параметры автокалибратора

```python
AutoCalibrator(
    min_days=30,              # Минимум дней для калибровки
    min_observations=1000,    # Минимум наблюдений на порог
    target_trigger_rate=0.02, # Целевая частота (2%)
    auto_apply=False          # Автоприменение
)
```

### Изменение параметров

В `main.py`:

```python
# Более агрессивная калибровка (раньше, чаще)
self.auto_calibrator = get_auto_calibrator(
    min_days=14,              # Калибровать через 2 недели
    target_trigger_rate=0.05, # Более частые срабатывания (5%)
    auto_apply=True           # Автоприменение
)

# Более консервативная (позже, реже)
self.auto_calibrator = get_auto_calibrator(
    min_days=60,              # Калибровать через 2 месяца
    target_trigger_rate=0.01, # Редкие срабатывания (1%)
    auto_apply=False          # Только рекомендации
)
```

---

## ⚠️ Важные моменты

### 1. Первая калибровка - самая важная

После 30 дней система впервые "увидит" реальность. Проверь рекомендации внимательно!

### 2. Auto-apply с осторожностью

Включай `auto_apply=True` только после того, как убедишься что первые 2-3 калибровки были правильными.

### 3. Сезонность

Система учитывает только накопленные данные. Если запустил зимой - летом может потребоваться ре-калибровка.

### 4. Резкие изменения

Если условия резко изменились (новый сенсор, другая локация) - лучше сбросить данные и начать заново.

---

## 🎯 Итого

### Что получаем

✅ **Автоматическая оптимизация** - система сама находит лучшие пороги  
✅ **Непрерывное улучшение** - чем дольше работает, тем точнее  
✅ **Честные вероятности** - через 3-6 месяцев можно показывать реальные p-values  
✅ **Адаптация** - система подстраивается под изменения  
✅ **Прозрачность** - все изменения логируются и объясняются  

### Что НЕ получаем

❌ **Мгновенную точность** - нужно минимум 30 дней  
❌ **Магию** - система не предсказывает будущее, только оптимизирует пороги  
❌ **100% автоматизм** - критичные изменения требуют проверки  

---

**Дата создания:** 2024-12-14  
**Статус:** ✅ Готово к использованию

**Первая калибровка:** Через 30 дней после запуска  
**Режим:** Suggest only (безопасный)

