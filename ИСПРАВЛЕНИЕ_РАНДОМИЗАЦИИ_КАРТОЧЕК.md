# Исправление: Карточки Больше Не Рандомизируются

**Дата**: 18 января 2026  
**Статус**: ✅ Исправлено

## Проблема

Карточки Predictions в PWA постоянно менялись:
- Показывало одно условие (L1_crypto) в 00:18
- Через 2 минуты показывало другое (L1_quantum_rng) в 00:20
- Потом третье (L1_space_weather) в 00:22
- Старые карточки исчезали

## Причина

### Старая Логика (НЕПРАВИЛЬНО)
```python
def _save_predictions_to_file(self, condition: Condition, probabilities: dict):
    # Создать predictions_list для ЭТОГО условия
    predictions_list = [...]
    
    # ПЕРЕЗАПИСАТЬ весь файл
    data = {"predictions": predictions_list[:20]}
    with open(file, "w") as f:
        json.dump(data, f)
```

**Что происходило**:
1. Аномалия `L1_crypto` в 00:18 → сохранили 1 предсказание
2. Аномалия `L1_quantum_rng` в 00:20 → **ЗАМЕНИЛИ** на 1 новое предсказание
3. Аномалия `L1_space_weather` в 00:22 → **ЗАМЕНИЛИ** на 1 новое предсказание
4. Результат: всегда показывается только ПОСЛЕДНЕЕ условие

### Почему Это Плохо

**Для пользователя**:
- Карточки "прыгают" каждые 2-5 минут
- Невозможно отследить несколько условий одновременно
- Теряется контекст (что было раньше)

**Для трейдера**:
- Не видит все активные сигналы
- Может пропустить важное предсказание
- Нет полной картины

## Решение

### Новая Логика (ПРАВИЛЬНО)
```python
def _save_predictions_to_file(self, condition: Condition, probabilities: dict):
    # 1. Загрузить существующие предсказания
    existing_predictions = load_from_file()
    
    # 2. Создать новые предсказания для ЭТОГО условия
    new_predictions = [...]
    
    # 3. ОБЪЕДИНИТЬ: удалить дубликаты по ID, добавить новые
    merged = remove_duplicates(existing_predictions, new_predictions)
    
    # 4. Фильтровать старые (>24ч)
    active = filter_by_time(merged, cutoff=24h)
    
    # 5. Сохранить ВСЕ активные предсказания
    save_to_file(active)
```

**Что происходит теперь**:
1. Аномалия `L1_crypto` в 00:18 → добавили 1 предсказание (всего: 1)
2. Аномалия `L1_quantum_rng` в 00:20 → добавили 1 предсказание (всего: 2)
3. Аномалия `L1_space_weather` в 00:22 → добавили 1 предсказание (всего: 3)
4. Результат: показываются ВСЕ 3 условия одновременно!

## Технические Детали

### 1. Загрузка Существующих
```python
existing_predictions = []
if predictions_file.exists():
    with open(predictions_file, 'r') as f:
        data = json.load(f)
        existing_predictions = data.get("predictions", [])
```

### 2. Дедупликация по ID
```python
# ID формат: "L1_crypto_earthquake_moderate"
pred_id = f"{condition.to_key()}_{event_type}"

# Удалить старые предсказания с тем же ID
existing_ids = {p["id"] for p in new_predictions}
merged = [p for p in existing_predictions if p["id"] not in existing_ids]
merged.extend(new_predictions)
```

**Зачем**: если условие `L1_crypto` появилось снова, обновляем его timestamp, но не дублируем.

### 3. Фильтрация по Времени
```python
cutoff = current_time - (24 * 3600)
active_predictions = [
    p for p in merged_predictions 
    if p.get("timestamp", 0) > cutoff
]
```

**Зачем**: автоматически удаляем устаревшие предсказания (старше 24 часов).

### 4. Сортировка
```python
active_predictions.sort(key=lambda x: (-x["probability"], x["avg_time_hours"]))
```

**Порядок**:
1. Сначала по вероятности (100% → 40%)
2. Потом по времени (быстрые → медленные)

### 5. Лимит
```python
data = {"predictions": active_predictions[:50]}  # Top 50 (было 20)
```

**Увеличено**: с 20 до 50, чтобы показывать больше условий.

## Результат

### До (Плохо)
```
00:18 → Показывает: L1_crypto (1 карточка)
00:20 → Показывает: L1_quantum_rng (1 карточка) ← L1_crypto исчез!
00:22 → Показывает: L1_space_weather (1 карточка) ← все предыдущие исчезли!
```

### После (Хорошо)
```
00:18 → Показывает: L1_crypto (1 карточка)
00:20 → Показывает: L1_crypto, L1_quantum_rng (2 карточки)
00:22 → Показывает: L1_crypto, L1_quantum_rng, L1_space_weather (3 карточки)
```

### Пример PWA
```
┌────────────────────────────────────────────────┐
│ 🌍  Earthquake M5.0+                    100%   │
│ 🕒 Today · 00:18                               │
│ When crypto shows unusual activity...          │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│ 🌍  Earthquake M5.0+                    100%   │
│ 🕒 Today · 00:20                               │
│ When quantum sensors show anomalies...         │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│ 🌍  Earthquake M5.0+                    100%   │
│ 🕒 Today · 00:22                               │
│ When space weather shows unusual activity...   │
└────────────────────────────────────────────────┘
```

**Все 3 карточки стабильны!** ✅

## Автоматическая Очистка

Каждые 60 секунд `_refresh_predictions_file()` удаляет устаревшие:
```python
cutoff = current_time - (24 * 3600)
active = [p for p in predictions if p["timestamp"] > cutoff]
```

**Результат**: карточки автоматически исчезают через 24 часа после обнаружения.

## Преимущества

✅ **Стабильность** - карточки не прыгают  
✅ **Полная картина** - видны все активные условия  
✅ **Для трейдеров** - не пропустишь важный сигнал  
✅ **Автоочистка** - старые предсказания удаляются автоматически  
✅ **Масштабируемость** - до 50 предсказаний одновременно  

## Качество на Первом Месте! 🎯

Теперь система показывает **все активные предсказания**, а не только последнее. Это именно то, что нужно для публичной системы!
